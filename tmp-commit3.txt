任务3 验收通过：首次引导、降级提示与减少动效入口

- 落地页新增手势引导区（GestureGuide + gestureOnboarding 单一数据源）：四张卡=手绘 SVG 线稿
  + 手势名 + [按键] 样式名，标题与保持时长由 HOLD_MS 推导，避免文案与阈值各说各话
- 摄像头降级提示（CameraFallbackNotice + cameraFallback）：覆盖 denied/unavailable/
  unsupported/error/closed，每条=温和标题 + 安抚 + 三条可操作步骤，可手动收起、14 秒自动退场
- 减少动效/低性能可被使用者知晓：画质标记带 data-motion，reduce 时显示「减少动效」，
  与纯性能降档可区分；模式入口下加一行极轻常驻说明，未引入设置面板
- 1440x900 下首屏完整性：加 max-height:880px 断点收缩留白，实测 scrollHeight == innerHeight

验证：npm test 8 文件 / 129 项全通过；lint 0 错误；tsc -b + vite build 通过；
tools/probe.mjs 17/17；工具 landingCheck 实测无滚动、CTA 可见、引导区与 CTA 无重叠、页脚在首屏内。
降级分支由一次性 CDP 探针真实触发（替换 navigator.mediaDevices 五种失败）16/16 通过。

遗留：无真实摄像头，识别率与「保持进度」仍无法端到端验证。
