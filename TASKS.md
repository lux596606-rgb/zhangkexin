# 张珂欣生日祝福互动网站 · 总 PM 任务规划与验收记录

> 依据：`C:\Users\LL\Desktop\生日祝福互动网站-需求整理.md`
> 执行方式：总 PM 拆解任务 → 派发子 agent → 验收 → 再规划下一个任务（一次只跑一个写入任务，避免文件冲突）

## 0. 已锁定的业务决策（本次会话确认）

| 项 | 结论 | 对应文档条目 |
| --- | --- | --- |
| 音乐形态 | 首版沿用现有 Web Audio 合成《Happy Birthday》旋律，**不阻塞开发**；真实音频素材后续单独派发替换任务 | 3.5 / 5 |
| 部署 | 服务器已就绪，SSH/面板信息后续提供；先交付可部署的构建产物 + 部署说明，暂不触碰服务器 | 9.3 |
| 聊天回忆/彩蛋 | 首版不加，只用通用温柔祝福文案；内容由用户后续提供 | 9.1 |
| 制作人署名 | 不显示 | 4 |

## 1. 现状盘点（PM 审计结论）

- 技术栈：React 19 + TypeScript + Vite 8 + Oxlint + Vitest，MediaPipe Gesture Recognizer 已本地化到 `public/mediapipe/`（不依赖 CDN）。
- 已具备：四样式映射（银河/生日快乐/猪头/收束）、键盘 1-4 / 空格 / R、鼠标点击四入口、银河态滚轮与拖拽、摄像头授权与拒绝降级、手势保持/冷却/释放状态机（有单测）、猪头图轮廓与色块采样粒子、静音/暂停音乐。
- 基线质量：`npm run lint` 0 错误、`npm test` 11 项通过、`dist/` 已产出。
- **主要缺口（对照文档逐条）**：
  1. 3.3 / 9.3.4「根据设备性能自动调整粒子数量与画质」——当前只有一次性静态启发式，没有实时帧率反馈的自适应。
  2. 3.3「状态切换需有缓动和过渡，避免突然跳变」——样式切换是直接重设目标，无过渡编排，文字/收束几乎瞬移。
  3. 5 / 9.1.2「猪图案可加轻微呼吸、耳朵小幅弹动，但不破坏基础表情」——未实现。
  4. 3.2「`2` 生日快乐：文字保持清晰并持续呼吸」——无呼吸效果（仅全屏闪烁）。
  5. 3.2「`4` 祝福收束：星光聚拢 + 轻量烟花」——仅有静态光环，无烟花。
  6. 4 / 8「首次进入、授权失败、识别中、识别不到等状态需明确反馈」「操作说明简短」——无首次进入手势/键盘引导层（App 内只有一行 footer 提示）。
  7. 6 / 8「无摄像头/低性能模式」「减少动态效果替代路径」——无显式降级策略与提示。
  8. 6「Chrome/Edge/Safari 近期版本」——无兼容性矩阵与实测记录。
  9. 9.3 部署——无部署文档；`vite.config.ts` 未设置 `base`，子路径部署会白屏。

## 2. 任务分解与状态

| # | 任务 | 覆盖文档条目 | 状态 |
| --- | --- | --- | --- |
| 1 | 粒子核心：实时性能自适应 + 平滑过渡编排 + 猪头呼吸/耳朵弹动 + 生日文字呼吸 + 轻量烟花 | 3.3、3.5、5、9.1.2、9.3.4 | ✅ 验收通过（含 1b/1c 修复） |
| 2 | 手势识别鲁棒性：捏合判定加固、抖动抑制、识别反馈可视化 | 3.2、8、10 | ✅ 验收通过（含 2b 收尾） |
| 3 | 首次进入引导与状态反馈层：手势/键盘提示、降级与减少动效路径 | 3.1、8 | 待规划 |
| 4 | 文案与祝福内容定稿（主文案、分段短句、猪图案说明文案） | 3.5、5 | 待规划 |
| 5 | 兼容性与性能实测（Chrome/Edge/Safari，低性能设备降级） | 6、9.3.4 | 待规划 |
| 6 | 发布准备：构建产物 + HTTPS 部署说明（等用户给服务器信息后可追加实际部署） | 9.3 | 待规划 |
| 7 | 工程净化：自托管字体（去掉 Google Fonts 外链）、清理脚手架残留资源 | 6、7 | 待规划 |

## 3. 验收记录

### 任务 1（粒子核心）
- 派发时间：本次会话
- 自动检查：`npm run lint` 0 错误、`npm test` 62 项通过、`npm run build` 成功 —— **形式验收通过**
- 浏览器冒烟（`tools/probe.mjs`）：17/17 通过 —— 落地页、四样式切换、空格/R、鼠标入口、MediaPipe 正常加载、无未捕获异常
- **实质验收结论：不通过。** 截图与像素分析显示：`生日快乐` 与 `猪头卡通` 根本没有成形，粒子只是随机噪点。
  - 证据：点亮像素包围盒铺满整张画布（`100x100@0,0`）、12×12 能量分布全为 `.`、中心区质量占比仅 ~30%（等于均匀分布）。
  - 反证：`祝福收束` 在同一次运行中出现过正确成形（`bbox≈397×302`、中心区质量占比 83.8%、能量图可辨文字块），说明形状采样与目标绑定本身是通的，问题在收敛环节。
  - 教训：`probe.mjs` 那类"元素存在/类名切换"断言只能证明接线，**不能证明画面正确**；必须配合像素级判据。此教训已写入 `tools/README.md`。
- 根因（CDP 逐帧取证）：**动画时钟停摆**。effect 挂载路径上存在两条各自 `requestAnimationFrame(renderFrame)` 的调度（effect 末尾 + `applyTargets` 内部），叠加 StrictMode 双挂载形成多个渲染循环；它们共享 `lastTimeRef`，同一次 rAF tick 内多个回调拿到相同 timestamp，后者算出 `delta = 0` 并被 `clamp` 钳住。于是 `transition.advance(0)` → 过渡 `elapsed` 恒为 0 → `gatherWeight()` 恒 0 → `follow = 0` → 粒子永不收拢。取证数据：14 秒内 3192 个不同 `renderFrame` 闭包、2127 次同 timestamp 重复渲染、`tElapsed` 恒 0、到目标平均距离恒 244。
- 附带缺陷：猪头参考图 1254×1254 但内容有效区远小于画布，`samplePig()` 按整图 fit，导致猪头仅占画布宽度约 53%，即使成形也偏小。
- 处置：派发**任务 1b**（修复时钟单调性 + 循环单所有者 + 猪头按 alpha 内容框适配），并要求提交真实浏览器自证。

### 任务 1b（修复粒子不成形 + 猪头尺寸）
- 派发时间：本次会话
- 独立验收结论：**部分通过**
  - ✅ **时钟停摆已修复（P0 通过）**。我的验证：`生日快乐` 点亮像素包围盒 211×218、中心区质量占比 100%、能量图可辨两行文字块；截图里「生日 / 快乐」逐字可读。数值上 `tElapsed` 恒为 0 的问题消失（子 agent 报告 0.558s→1.45s、`follow` 0→0.43、到目标平均距离 252→1~3），与画面一致。
  - ✅ **猪头裁剪几何已修复（P1 通过）**。我用离线同源算法复核：内容框 843×656 → 缩放 0.543 → 落点 (95,88) → 绘制 458×356，居中且占据画面主体（修复前仅 342 宽 / 约 53%）。
  - ❌ **猪头仍不可辨识（P2 不通过）**。我打开在线截图看到的是「一条亮带 + 均匀噪点团」，不是猪脸（子 agent 报告"清晰可辨的正脸猪头"，与实际画面不符）。
- 根因（我用离线同源复现 + 对照实验定位）：**粒子分配策略问题，不是采样/裁剪问题**。
  - 采样点按亮度分布：深色（轮廓+五官）6054 个 = **19%**，浅色粉色面填充 26441 个 = **81%**。
  - `resample()` 是**均匀抽取**，等于按面积把粒子平摊到整个脸盘 → 均匀点阵/摩尔纹，轮廓与五官被稀释。
  - 对照实验（离线渲染 PNG 亲眼比对）：均匀 1379 点 = 噪点团；均匀 4200 点 = 有轮廓但脸面是均匀点阵；**加权抽取 2400 点 = 轮廓清晰、眼睛/猪鼻/腮红可辨的一只正脸猪头**；加权 1379 点也已能认出。→ 结论：加权抽稀优于无脑加粒子。
- 处置：派发**任务 1c**（`resample` 支持权重，猪头按亮度加权，文字样式行为保持不变；并要求可复核证据 + 新增单测）。
- 经验教训（写入 `tools/README.md`）：验收必须包含"离线同源复现 + 亲眼读图"，不能只信实现者的文字描述。

### 任务 1c（猪头可辨识度）
- 派发时间：本次会话
- 独立验收结论：**通过**
- 关键新发现（子 agent 定位，我已认可）：原 `priority` 点共 1458 个，其中 **97.1%（1416 个）是贴着轮廓外侧的白背景光晕**，只有极少数是真眼睛白点。旧 `resample` 把 14% 预算给了它最上面一段 —— **这正是我先后两次看到"一条亮带"的成因**。修复后 priority 只剩 36 个真眼睛白点，加权路径保留 8 个（每眼 4 个）。
- 分配策略：权重 `max(0.02, (1-luma/255)^2.2)`，按累积权重等间距取样 + 确定性抖动。实测（真实模块 + 真实参考图，浏览器内）：采样 32333 点，深色 5967 = 18.5%；high 档 2234 粒子中加权分到 **深色 78.2% / 浅色 21.8%**，而均匀抽取只有深色 17.6%。
- 我的独立复核（亲眼看图）：
  - `where-猪头卡通.png` 与 2 倍放大图：两只耳朵（含内折线）、两只眼睛（深色团各带白点）、中间椭圆猪鼻（两个鼻孔）、腮红、圆头轮廓**全部可辨**。
  - `where-祝福收束.png`：粒子拼出「生日快乐，张珂欣」清晰可读，带烟花与光晕。
  - `where-生日快乐.png`：两行「生日 / 快乐」逐字可读。
- 数值复核：猪头 bbox 445×350（画布 648×532，居中）、中心区质量占比 43%、粉系像素 99.8%。
- **我补充的边界条件验证（子 agent 未覆盖）**：CPU 节流把画质压到 low 档后（`tools/tierCheck.mjs`），猪头 bbox 换算回 CSS 像素为 463×362 —— **与 high 档同量级**（先前 278×217 的读数只是 DPR 0.6 的设备像素坐标，不是形状缩小）。低档截图里猪头**仍然可辨识**（耳朵/猪鼻/眼睛都在，只是颗粒更粗），自适应降档机制真实生效（HIGH 218FPS → LOW 27FPS）。
- 附带说明：`birthday`/`closing` 也拿到了 1.5× 粒子密度（仍为均匀抽取，几何不变）。经复核属可接受：文字更饱满，且自适应档位会兜底性能。
- 质量门：lint 0 错误（1 条 warning 出自我自己的 `tools/converge.mjs`，已清理）、**74/74 单测通过**（新增 12 条加权抽稀单测）、**tsc + vite build 通过**。
- **git 存档点**：`61e63ea` 初始提交（工作区干净，`tmp-artifacts/` 已加入 gitignore）。

### 任务 1 最终结论
- 状态：**验收通过**。四种样式均已由 PM 肉眼确认可辨识；时钟停摆、裁剪几何、加权分配三个问题全部闭环。
- 遗留的非阻塞项（转后续任务）：① `index.css` 仍从 Google Fonts 联网取字体（部署风险）；② `src/assets/hero.png`、`react.svg`、`vite.svg`、`public/icons.svg` 为脚手架残留；③ 手势鲁棒性、首次引导、兼容性实测、部署文档尚未做（任务 2~6）。

### 任务 2（手势鲁棒性与反馈）
- 派发时间：本次会话
- 覆盖文档条目：3.2（手势规则）、8（状态反馈与误识别不可逆）、10（需要控制的重点）
- 过程：首轮交付**未通过**（子 agent 未给报告即结束，`npm test` 12 项失败，仓库根目录残留 `tmp-debug.test.mts`）。派发 2b 收尾后通过。
- **PM 根因诊断（首轮失败原因）**：实现本身没问题，是**测试夹具几何不自洽**，用例在数学上不可能通过。
  - `pinchingHand()` 食指 PIP 实测 139° < 145° 阈值 → 判为"未伸直"；`fistHand()` 号称握拳，实测四指 PIP 167° / 腕比值 1.43 —— **其实是四指全伸直**，所以被正确判成捏合，而用例期望 null。
  - PM 另修好既有用例 `pinchLandmarks()` 的同类问题：食指 MCP 被留在掌心默认值 → 5-6-8 三点共线 → 夹角退化成 `NaN` → 伸直判定不成立。修正后该文件恢复 11/11。
- **实现最终改动**：仅 1 个常量 `FINGER_EXTENSION_MIN_ANGLE_DEG` 145°→130°（平滑 / 抖动 / 冷却逻辑一行未动）。
  - 依据：145° 那一档要让拇指食指指尖贴合，需要 1.10 掌尺度的拇指（真人 0.66~0.71），**真实捏合会被系统性漏判**；扫描显示 75°~180° 区间内角度是唯一约束（腕-指尖/腕-PIP 要到 70° 才跌破 1.02）。
  - 安全边界已用测试钉死：夹具改写为前向运动学生成（写的角度 = 关键点实测角度，无共线退化）；握拳夹具 PIP 40°、腕比值 0.86 < 1.02（两条判据同时踩掉）；新增「食指轻微弯曲 140° 仍成立（旧 145° 会漏判）」与「深弯 120° 不判捏合，且与握拳 40° 隔着 60° 以上」两条用例。
- **PM 独立验收**：
  - `npm test` → **5 个文件 / 110 项全通过**；`npm run lint` → 0 错误 0 警告；`npm run build` → 通过；`tools/probe.mjs` → **17/17**，无未捕获异常。
  - 读码复核：`PinchSmoother` 只消费同一次 `recognizeForVideo` 的关键点，**每帧仍只有一次推理**、无新增网络请求（符合需求 7 隐私条）；`GestureFeedback` 走命令式 DOM 更新 + 状态订阅，避免每帧 `setState` 拖慢粒子画布；300ms 换样式闸门**先判"是否同目标"再判冷却**，因此不挡需求 3.4.3 的重复进入。
  - 新增浏览器验收 `tools/feedbackCheck.mjs`：反馈条只在体验页出现、`aria-label` 完整、状态与顶部状态行一致、位于舞台左上角不遮挡居中主体。
- 已知限制（如实记录）：无真实手部，无法端到端验证识别率；反馈条的**保持进度**分支未能在浏览器实测（需真实手势进入 holding）。**捏合阈值仍需用真实摄像头校准**，已列入上线前任务。

## 4. 项目当前快照

- git 存档点：`61e63ea`（初始提交）；任务 2 另有存档提交
- 质量门：lint 0 错误、**110 项单测通过**、`tsc -b && vite build` 通过
- 真实浏览器验收：`tools/probe.mjs` 17/17；四种样式均经 PM 肉眼确认可辨识；手势反馈条经 `tools/feedbackCheck.mjs` 确认
- dev server：`npm run dev -- --port 5180 --strictPort --host 127.0.0.1`（摄像头需 localhost 安全上下文）
