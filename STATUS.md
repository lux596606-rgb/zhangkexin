# 交付状态与下一步（给站长/验收人看）

> 这份文档面向"要把它上线的人"。技术细节与逐轮验收记录见 `TASKS.md`，部署步骤见 `DEPLOY.md`。

## 一、现在可以做什么

网站功能已完整实现并逐项验收通过，**可以部署**。当前质量门：

| 项目 | 结果 |
| --- | --- |
| 单元测试 | **135 项全通过**（9 个测试文件） |
| 代码检查 | lint 0 错误 0 警告 |
| 生产构建 | `tsc -b && vite build` 通过 |
| 浏览器实测 | 真实 Chrome 冒烟 **17/17**；四种动画样式均已肉眼确认可辨识 |
| 产物自检 | `npm run check:dist` **11/11** |
| 外网依赖 | **0**（字体已自托管，不依赖 Google Fonts 等任何外部服务） |
| 产物体积 | **20.79 MB / 11 文件**（优化前 43.07 MB） |

四种样式均已做过像素级与肉眼双重确认：银河态、生日快乐（粒子文字可读）、猪头卡通（耳朵/眼睛/猪鼻/腮红可辨）、祝福收束（文字 + 烟花）。

## 二、你必须做的三件事（我无法代做）

### 1）用真实摄像头复测手势（刚刚做过一轮修复，请重新过一遍）

**背景**：你在 Windows 上实测发现「捏合完全无法识别」+「所有手势有时没反应」。已定位到两个确定性缺陷并修复：

1. **捏合要求"其余三指至少两指伸直"** —— 但真人捏合时中指/无名指/小指是自然弯曲的，这条**永远不成立**。而另外三个手势走 MediaPipe 内置识别、只有捏合是自研几何判定，所以**只有捏合全灭**，完全对应你的现象。已删除该约束。
2. **保持窗口中途掉一帧就清零** —— 真实识别有抖动，700ms 进度永远攒不满 → **所有手势"有时没反应"**。已加入 150ms 的丢失容忍（丢失只冻结进度、不白送时间，仍必须真正识别满 700ms）。

**请重新测一遍**（dev server：`npm run dev -- --port 5180 --strictPort --host 127.0.0.1`）：

1. 主页面 `http://127.0.0.1:5180/`：四个手势各做 2~3 次，确认**捏合现在能触发「生日快乐」**、其他三个仍正常、并且**握拳不会被误判成捏合**。
2. 如果还有问题，打开校准页 `http://127.0.0.1:5180/calibration.html`：
   - 做到"差一点成立"时，页面会**直接告诉你卡在哪一条**（食指 PIP 角度不够 / 腕比值不够 / 两指还不够近）；
   - 点「开始采样」做几次捏合，再点「停止采样并出统计」，把报告整段贴回给我。
3. **仍然需要留意的边界**：若真人捏合时食指弯到 **130° 以下**，仍会被判为"未伸直"而漏判 —— 这种情况只有校准页的采样数据能告诉我，届时再调阈值。

### 2）Safari 实测（你已决定自行找 Mac 验证）

需求文档把 Safari 列为目标浏览器，但我在 Windows 上**无法真实验证 Safari**。

- 当前状态：**Safari 未验证**，请在交付说明/心里有数。
- 需要重点确认的：摄像头授权弹窗是否正常、手势识别能否加载（Safari 对 WASM 与 `getUserMedia` 的行为与 Chrome 有差异）、粒子动画帧率。
- 如果 Safari 上手势不可用：页面会显示「手势识别不可用 · 可用键盘或鼠标」，键盘与鼠标仍能走完整段祝福 —— 这是设计好的降级路径，不是故障。

### 3）部署到 HTTPS（步骤见 `DEPLOY.md`）

- **必须 HTTPS**：浏览器只在安全上下文下允许摄像头，`http://` 访问拿不到摄像头权限。
- 上传 **`dist/` 里的内容**（不是 `dist` 目录本身）到网站根目录或子目录。
- `DEPLOY.md` 里有可直接抄的 Nginx 配置，含一个**关键点**：`.wasm` 必须返回 `application/wasm` MIME，否则 WASM 加载失败、手势识别不可用。
- 部署后按 `DEPLOY.md` 的 **11 条自检清单**逐条过一遍。

## 三、已知限制（如实列出，不隐瞒）

| 限制 | 影响 | 是否阻塞上线 |
| --- | --- | --- |
| 捏合阈值（食指伸直 130°）仍基于几何推算 | 若真人捏合时食指弯到 130° 以下仍会漏判 | 否，已修复主因；残留边界需校准页采样确认 |
| Safari 未实测 | 未知 | 否（有键盘/鼠标降级） |
| 不支持 SIMD 的旧浏览器无法用 wasm | 手势不可用，键盘鼠标可用，不白屏 | 否（目标平台 Chrome/Edge/Safari 近期版都支持 SIMD） |
| 首次进入体验需拉约 19 MB（wasm 11.21 MB + 模型 8.0 MB） | 首次加载偏慢 | 否，但建议上线后在真实网络下测一次耗时 |
| 矮屏（可视高度 ≤ 780px）底部会有约 44px 的轻微滚动 | 需要滚一下才看到完整模式入口 | 否（1440×900 及以上基本不影响） |
| 单帧把某手势高置信度误判成另一类别时，会重置当前保持进度 | 偶发需重做一次手势 | 否（属已知边界，未处理） |

## 四、日常操作备忘

```bash
npm run dev              # 开发（摄像头需 localhost）
npm run build            # 生产构建 → dist/
npm run check:dist       # 构建产物自检（11 项）
npm test                 # 单元测试（135 项）
npm run lint             # 代码检查
```

- 校准页：`http://127.0.0.1:5180/calibration.html`（仅本机工具，不进产物、不入库）
- 验收工具（PM 用，可随时删）：见 `tools/README.md`
- 修改粒子/手势相关代码后，**不要只看测试通过**——必须用 `tools/where.mjs` 看形状是否真的成形、并亲眼看截图。这是本项目踩过两次的坑：测试全绿但画面上根本没有形状。
